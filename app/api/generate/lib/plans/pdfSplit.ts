import type { PlanPageImage, PlanUpload } from "./types"

export async function splitPlanUploadsToPages(
  uploads: PlanUpload[]
): Promise<PlanPageImage[]> {
  // PDF/image normalization goes here
  // return one array of normalized page images
  return []
}